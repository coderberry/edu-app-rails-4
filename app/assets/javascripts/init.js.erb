function imgError(image){
  image.onerror = "";
  image.src = "<%= asset_path("no-image.png") %>";
  return true;
}

$(function() {
  var flash = $('#flash-view #message');
  if (flash) {
    flash.delay(3000).fadeToggle(1000);
  }

  $('*[data-toggle="tooltip"]').tooltip({});
})
