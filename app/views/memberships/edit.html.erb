<div class="main-content">
  <div class="header clearfix">
    <h3 class="page-header">Edit Membership for <%= @membership.user.name %></h3>
  </div>

  <ul class="breadcrumb">
    <li><%= link_to "Organizations", organizations_url %></li>
    <li><%= link_to @organization, organization_path(@organization) %></li>
    <li>Memberships</li>
    <li class="active"><%= @membership.user.name %></li>
  </ul>

  <%= render 'form' %>
</div>